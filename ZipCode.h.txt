
#include<iostream>
#ifndef ZIPCODE_H
#define ZIPCODE_H

class ZipCode{
public:
	ZipCode(int);
	ZipCode(string);
    void setcode(int);
	void setcode2(string);
	void seperate();    //將輸入integer拆開分別存入陣列
	void encode();
	void decode();
	void transform();   //change string into integer
	int check1();
	int check2();       //檢查input string是否有誤
	void display1();   //輸出zipcode integer轉為 barcode string結果
	void display2();   //輸出barcode string轉為zipcode integer結果
		
private:
	int Code1;
	string Code2;
	int array1[5];      //用來存放拆解後的數字
	int array2[30];     //將code2由string轉回int存在array2裡
	int enzipcode[30];     //存放incode後的值
	int dezipcode[5];      //在此存放解碼後的code
	int flag;         //用來檢測是否出現非法數字
	

};
#endif